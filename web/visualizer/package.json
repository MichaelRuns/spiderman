{
  "name": "@spiderman/visualizer",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "GitHub-Pages-deployed inference visualizer: real autoregressive generation with @spiderman/nn, rendered on a plain 2D canvas as a classic layer-by-layer architecture diagram.",
  "scripts": {
    "predev": "node scripts/copy-weights.mjs",
    "dev": "vite",
    "prebuild": "node scripts/copy-weights.mjs",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@spiderman/nn": "workspace:*",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "@vitejs/plugin-react": "^4.3.4",
    "typescript": "^5.7.2",
    "vite": "^6.0.3",
    "vitest": "^3.0.5"
  }
}
