# Python
__pycache__/
*.pyc
.venv/
.pytest_cache/
*.egg-info/

# uv
uv.lock.tmp

# Raw/intermediate training data and checkpoints — large, reproducible from a
# data source + training script, not repo content.
llm/data/*
!llm/data/.gitkeep
llm/checkpoints/

# Node / JS (for the future web/ tooling)
node_modules/
dist/
.env

# Trained weights are large binaries — export/publish deliberately
web/weights/*
!web/weights/.gitkeep

# OS / editor
.DS_Store
.vscode/
.idea/
