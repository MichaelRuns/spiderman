"""spiderman_llm: a from-scratch LLM implementation.

Home for the model code migrated from the cs336 coursework: RoPE,
self-attention, transformer blocks, etc. in ``nn.py``; the BPE tokenizer in
``tokenizer.py``; training entry points in ``scripts/``.
"""

__version__ = "0.1.0"
