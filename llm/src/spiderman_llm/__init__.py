"""spiderman_llm: a from-scratch LLM implementation.

Home for the model code migrated from the cs336 coursework: RoPE,
self-attention, transformer blocks, the BPE tokenizer, optimizer, etc.
Modules live in ``nn.py``; training entry points live in ``scripts/``.
"""

__version__ = "0.1.0"
